nginx-proxy:
    image: jwilder/nginx-proxy
    container_name: dc-proxy
    ports:
      - 80:80
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
db:
    image: mysql
    container_name: dc-mysql
    hostname: mysql
    ports:
        - 3307:3306
    environment:
        MYSQL_ROOT_PASSWORD: password
        MYSQL_DATABASE: symfony
        MYSQL_USER: dev
        MYSQL_PASSWORD: password
    #volumes:
     #   - "./.data/db:/var/lib/mysql"
rabbitmq:
    build: rabbit
    container_name: dc-rabbit
    environment:
        VIRTUAL_HOST: rabbit.docker
        VIRTUAL_PORT: 15672
    #hostname: rabbit #fix understand why hostname is incompatible with nginx proxy service
    ports:
      - 15672:15672
      - 5672:5672
      - 25672:25672
      - 4369:4369
mailhog:
  image: mailhog/mailhog
  container_name: dc-mail
  environment:
    VIRTUAL_HOST: mail.docker
    VIRTUAL_PORT: 8025
  hostname: mailhog
  ports:
    - 8026:8025
webserver:
    build: webserver
    container_name: dc-api
    environment:
        VIRTUAL_HOST: api.docker
    ports:
        - 81:80
    volumes:
        - ./sf4:/var/www/html:rw
    links:
        - db:mysql
        - rabbitmq
        - mailhog
    tty: true
pma:
  image: phpmyadmin/phpmyadmin
  container_name: dc-pma
  links:
    - db:mysql
  ports:
    - 8181:80
  environment:
    VIRTUAL_HOST: pma.docker
    PMA_HOST: mysql
    PMA_USER: root
    PMA_PASSWORD: password
